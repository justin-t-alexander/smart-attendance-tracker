// postcss.config.mjs
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};

export default config;

// This configuration file sets up PostCSS with Tailwind CSS and Autoprefixer.